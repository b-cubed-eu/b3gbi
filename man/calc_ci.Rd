% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_ci_methods.R
\name{calc_ci}
\alias{calc_ci}
\alias{calc_ci.default}
\alias{calc_ci.total_occ}
\alias{calc_ci.occ_density}
\alias{calc_ci.spec_richness_density}
\alias{calc_ci.newness}
\alias{calc_ci.williams_evenness}
\alias{calc_ci.pielou_evenness}
\alias{calc_ci.ab_rarity}
\alias{calc_ci.area_rarity}
\alias{calc_ci.spec_occ}
\alias{calc_ci.spec_range}
\alias{calc_ci.hill0}
\alias{calc_ci.hill1}
\alias{calc_ci.hill2}
\title{Calculate Confidence Intervals for a Biodiversity Indicator}
\usage{
calc_ci(x, indicator, ...)

\method{calc_ci}{default}(x, indicator, ...)

\method{calc_ci}{total_occ}(x, indicator, num_bootstrap = 1000, ci_type = "perc", ...)

\method{calc_ci}{occ_density}(x, indicator, num_bootstrap = 1000, ci_type = "perc", ...)

\method{calc_ci}{spec_richness_density}(x, indicator, num_bootstrap = 1000, ci_type = "perc", ...)

\method{calc_ci}{newness}(x, indicator, num_bootstrap = 1000, ci_type = "perc", ...)

\method{calc_ci}{williams_evenness}(x, ...)

\method{calc_ci}{pielou_evenness}(x, ...)

\method{calc_ci}{ab_rarity}(x, indicator, num_bootstrap = 1000, ci_type = "perc", ...)

\method{calc_ci}{area_rarity}(x, indicator, num_bootstrap = 1000, ci_type = "perc", ...)

\method{calc_ci}{spec_occ}(x, indicator, num_bootstrap = 1000, ci_type = "perc", ...)

\method{calc_ci}{spec_range}(x, indicator, num_bootstrap = 1000, ci_type = "perc", ...)

\method{calc_ci}{hill0}(x, indicator, num_bootstrap = 1000, ...)

\method{calc_ci}{hill1}(x, indicator, num_bootstrap = 1000, ...)

\method{calc_ci}{hill2}(x, indicator, num_bootstrap = 1000, ...)
}
\arguments{
\item{x}{A data cube object}

\item{indicator}{An indicator calculated over time, in the form of a data
frame. *Note: this should NOT be an 'indicator_ts' object as it is meant to
be called by the 'compute_indicator_workflow' function.}

\item{...}{Additional arguments passed to specific calc_ci functions and
on to \code{boot::boot.ci()} (e.g., \code{conf}, \code{h}, \code{hinv}), or to the
\pkg{iNEXT}-based calculation for Hill numbers (e.g., \code{conf}).}

\item{num_bootstrap}{(Optional) Set the number of bootstraps to calculate for
generating confidence intervals. (Default: 1000)}

\item{ci_type}{(Optional) Type of bootstrap interval passed to
\code{boot::boot.ci()}. (Default: "perc")}
}
\value{
A data frame containing indicator values with calculated lower (\code{ll}) and upper (\code{ul}) confidence bounds.
}
\description{
Calculates indicator-level bootstrap confidence intervals. It
is called internally by \code{add_ci()} (indicator level) or when \code{ci_type} is
supplied to a \verb{*_ts()} function; it is not meant to be called directly.
}
\section{Methods (by class)}{
\itemize{
\item \code{calc_ci(total_occ)}: Calculate confidence intervals for total occurrences

\item \code{calc_ci(occ_density)}: Calculate confidence intervals for occurrence density

\item \code{calc_ci(spec_richness_density)}: Calculate confidence intervals for species richness density

\item \code{calc_ci(newness)}: Calculate confidence intervals for newness

\item \code{calc_ci(williams_evenness)}: Calculate confidence intervals for Williams' evenness

\item \code{calc_ci(pielou_evenness)}: Calculate confidence intervals for Pielou's evenness

\item \code{calc_ci(ab_rarity)}: Calculate confidence intervals for abundance-based rarity

\item \code{calc_ci(area_rarity)}: Calculate confidence intervals for area-based rarity

\item \code{calc_ci(spec_occ)}: Calculate confidence intervals for species occurrences

\item \code{calc_ci(spec_range)}: Calculate confidence intervals for species range

\item \code{calc_ci(hill0)}: Calculate confidence intervals for Hill0 (Richness)

\item \code{calc_ci(hill1)}: Calculate confidence intervals for Hill1 (Shannon)

\item \code{calc_ci(hill2)}: Calculate confidence intervals for Hill2 (Simpson)

}}
\examples{
\donttest{
# calc_ci() is called automatically when confidence intervals are requested
# during indicator calculation (or by add_ci(bootstrap_level = "indicator"))
occ_ts <- total_occ_ts(example_cube_1, first_year = 2000,
                       ci_type = "perc", num_bootstrap = 100)
head(occ_ts$data)
}
}
